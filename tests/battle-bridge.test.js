const test=require('node:test');const assert=require('node:assert/strict');const B=require('../battle-bridge.js');const G=require('../game-state.js');
test('start battle and setup carry hero and enemy armies',()=>{const s=G.createDefaultState();B.startBattle(s,{battleId:'x',enemyArmy:[{unitId:'skeleton',count:2,hpPer:10,totalHp:20,minDmg:1,maxDmg:2,speed:4,attack:3,defense:3,type:'skeleton'}],xpReward:100});const setup=B.getBattleSetup(s);assert.equal(setup.playerArmy.length,s.hero.army.length);assert.equal(setup.enemyArmy[0].count,2);});
test('victory persists surviving army and awards xp once',()=>{const s=G.createDefaultState(),before=s.hero.xp;B.startBattle(s,{battleId:'guard',enemyArmy:[{count:10}],xpReward:500,enemyObjectId:'guard'});B.recordResult(s,{outcome:'victory',survivingArmy:[{unitId:'skeleton',count:3,hpPer:10,totalHp:24}],defeatedModels:10});B.consumeAdventureResult(s);B.consumeAdventureResult(s);assert.equal(s.hero.army[0].totalHp,24);assert.equal(s.hero.xp,before+500);assert.deepEqual(s.map.defeatedArmyIds,['guard']);});
test('defeat marks scenario lost',()=>{const s=G.createDefaultState();B.startBattle(s,{battleId:'x',enemyArmy:[],xpReward:0});B.recordResult(s,{outcome:'defeat',survivingArmy:[]});B.consumeAdventureResult(s);assert.equal(s.scenarioStatus,'lost');});
test('no pending battle returns null setup for demo fallback',()=>{assert.equal(B.getBattleSetup(G.createDefaultState()),null);});
test('campaign stacks reserve v5 demo ids and all seven rows stay on board',()=>{const base={unitId:'skeleton',name:'Szkielet',type:'skeleton',count:1,hpPer:10,totalHp:10,minDmg:1,maxDmg:2,speed:4,attack:3,defense:3};const player=Array.from({length:7},(_,i)=>B.toBattleStack(base,'player',i));assert.equal(player[0].id,'p4');assert.ok(player.every(u=>u.r>=0&&u.r<11));assert.equal(B.toBattleStack(base,'ai',0).id,'a4');});
